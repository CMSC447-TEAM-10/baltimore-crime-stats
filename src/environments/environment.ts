// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBp8XlETCYMdjdbfCcxkI7dazcmbxcfd-k",
    authDomain: "cmsc447-team10.firebaseapp.com",
    databaseURL: "https://cmsc447-team10.firebaseio.com",
    projectId: "cmsc447-team10",
    storageBucket: "cmsc447-team10.appspot.com",
    messagingSenderId: "171203114583",
    appId: "1:171203114583:web:fea775234bc453cf5fd512",
    measurementId: "G-VYE4DLWNV3"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
